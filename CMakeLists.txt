# Copyright (c) BBP/EPFL 2014
#                        Stefan.Eilemann@epfl.ch
# All rights reserved. Do not distribute without further notice.

cmake_minimum_required(VERSION 2.8)
project(Fivox)
list(APPEND CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/CMake
  ${CMAKE_SOURCE_DIR}/CMake/common)
include(GitExternal)

set(RELEASE_VERSION OFF) # OFF or ABI version
set(LAST_RELEASE 1.0.0)
set(VERSION_MAJOR "0")
set(VERSION_MINOR "1")
set(VERSION_PATCH "0")

set(FIVOX_DESCRIPTION "Library to sample fields into regular volumes")
set(COMMON_USE_CXX03 ON)

include(Common)
include(FindPackages) # Generated by Buildyard and checked in
include(${ITK_USE_FILE})

add_subdirectory(fivox)
add_subdirectory(apps)
add_subdirectory(tests)

include(CPackConfig)
include(DoxygenRule)
